#include "x11_key_grab_set.h"

#include "common.h"

int X11KeyGrabSet::addGrab(std::string sequence)
{
    assert(0);
}

void X11KeyGrabSet::clear()
{
    assert(0);
}

int X11KeyGrabSet::find(const X11Global::KeySequence &sequence) const
{
    assert(0);
}
