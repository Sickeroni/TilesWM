#ifndef __COMMON_H__
#define __COMMON_H__

#include "types.h"
#include "debug_stream.h"

#include <iostream>
#include <assert.h>
#include <stdlib.h>

using std::cout;
using std::cerr;
using std::endl;

enum {
    INVALID_INDEX = -1
};

#endif // __COMMON_H__
